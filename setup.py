from distutils.core import setup

setup(name="ASVDb",
      version="0.1",
      packages=["asvdb",
                ],
      license="Apache 2.0",
      description='ASV "database" interface',
      )
